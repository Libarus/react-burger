//const logOn = false;

// export const loggerMiddleware = () => (next: any) => (action: any) => {
//     // Выводим в консоль время события и его содержание
//     if (logOn) console.log(`${new Date().getTime()} | Action: ${JSON.stringify(action)}`);
//     // Передаём событие «по конвейеру» дальше
//     return next(action);
// };
